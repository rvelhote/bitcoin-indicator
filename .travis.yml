language: python
python:
    - "3.5"
    - "3.6"
before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y python3-gi
install: "pip install -r requirements.txt"
script: python -m unittest bitcoin_test